module Paths_cabal_install where

import Data.Version (Version(..))

-- manually written to use cabal-install as a library

version :: Version
version = Version {versionBranch = [0,6,2], versionTags = []}

